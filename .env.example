# Required when the API is exposed outside your private machine.
# Replace this with a long random secret.
LEAD_API_KEY=replace-with-a-long-random-secret

# Public HTTPS URL used in API responses.
# Local example:
PUBLIC_BASE_URL=http://localhost:8080
# Production example:
# PUBLIC_BASE_URL=https://leads.example.com
